<Project>
  <Import Project="../../Directory.Build.targets" />

  <PropertyGroup>
    <RootNamespace>$(RootNamespace).FunctionFilters.$(MsBuildProjectName)</RootNamespace>
  </PropertyGroup>

</Project>
